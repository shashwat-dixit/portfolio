---
export const prerender = true;
import Layout from "../layouts/Layout.astro";
import HomePage from "@/components/HomePage";
import { CONFIG } from "@/data/config";
---

<Layout markdownURL={`${CONFIG.site.url}/index.md`}>
  <HomePage client:load />
</Layout>
